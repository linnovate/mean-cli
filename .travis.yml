language: node_js

cache:
  - apt
  - npm
  
matrix:
  fast_finish: true
  
env:
  - TASKER=grunt
  - TASKER=gulp
  
services: mongodb

before_install:
  - chmod u+x start.sh
  - ./start.sh test_self

script:
  - ./start.sh test_mean_init #make use of var:tasker

after_success:
  - ./start.sh test_navigation
