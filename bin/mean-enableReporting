#!/usr/bin/env node

'use strict';

var cli = require('../lib/cli'),
  program = require('commander');

program
  .description('Disable user reporting')
  .parse(process.argv);

cli.toggleReporting(true);
