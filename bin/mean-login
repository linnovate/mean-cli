#!/usr/bin/env node

'use strict';

var users = require('../lib/controllers/users'),
    program = require('commander');

program
  .description('Login to the network')
  .parse(process.argv);

users.login();
