machine:
  node:
    version: 0.10.22
dependencies:
  override:
    - npm install --dev
test:
  override:
  - chmod +x ./start.sh
  - chmod +x ./capture.sh
  - bash -c ./capture.sh
  - bash -c ./start.sh
  - cp /tmp/session.png $CIRCLE_ARTIFACTS
